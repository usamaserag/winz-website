[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for all routes (React Router handles them)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
